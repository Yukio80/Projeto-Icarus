name: Deploy Dashboard

on:
  push:
    branches: [master]
    paths:
      - 'web3-ai-dao/dashboard/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: curl -s -X POST '${{ secrets.VERCEL_DEPLOY_HOOK }}'
